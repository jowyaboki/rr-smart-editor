# Node Editor Architecture

Unifying major creative subsystems through a professional node-based visual workflow environment.

## Design Highlights
* **Infinite canvas grids**: SNap alignments, mini-maps, and drag interactions inside `WorkflowCanvas`.
* **SVG curved connections**: Rendered dynamically to illustrate sequential execution pipelines.
* **Ports validation**: compatible port checking directly linking outputs to inputs.
