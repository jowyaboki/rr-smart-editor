# AI Context Guide - Phase 10

The semantic index map describing components, hooks, endpoints, and database models.

## 1. Context Keys
* **Components**: `@ai-video-editor/ui` (reusable UI boundaries).
* **Themes**: `@ai-video-editor/design-system` (tokens and theme overrides).
* **State Management**: Zustand stores and temporal state history.
