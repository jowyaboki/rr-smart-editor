# Knowledge Graph Reference - Phase 10

Complete schema describing relationships between components, services, and endpoints.

## 1. Schema Node Layout
* React pages depend on `@ai-video-editor/ui`.
* UI components consume themes resolved by `@ai-video-editor/design-system`.
* App servers execute transactions via database SQL routers.
