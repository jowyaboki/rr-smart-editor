# Regression Scorecard - Phase 10

Scorecard tracking core regression checks.

## 1. Key Verification Checks
* **Backward Compatibility**: Existing v1.0 timelines continue to open, render, and export correctly.
* **Zustand Subscription Checks**: Zero state duplications or memory leak triggers observed during high-density scrubs.
* **Layout Presets**: Saved sidebar collapse, workspace width sizes, and layout choices hold across restarts.
* **Sandbox Verification**: Verified malicious package blocking scans.
