# Module Registration Guide

How to register custom visual panels and toolbar widgets dynamically inside the smart editor.

## Registration Form
* **Step 1**: Use the draggable `PluginsWorkspace` to specify custom plugin descriptors.
* **Step 2**: Clicking 'Install' dynamically registers and instantiates the component within standard workspace presets.
